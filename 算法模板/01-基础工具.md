# 01 · 基础工具（公共头）

> 通用 IO 与基础工具，不属于任何专题，作为整份模板的公共头。任何提交代码第一件事就是复制本文件内容。

## 快读模板（read / readll）

适用：大数据量输入必备，比 cin 快数倍。整份模板通用，复制到任意代码顶部。

复杂度：$O(n)$。

```cpp
#include <bits/stdc++.h>
using namespace std;

#define ll long long

inline int read()
{
    int x = 0, f = 1;
    char c = getchar();
    while (c < '0' || c > '9')
    {
        if (c == '-') f = -1;
        c = getchar();
    }
    while (c >= '0' && c <= '9')
    {
        x = x * 10 + c - '0';
        c = getchar();
    }
    return x * f;
}

inline ll readll()
{
    ll x = 0, f = 1;
    char c = getchar();
    while (c < '0' || c > '9')
    {
        if (c == '-') f = -1;
        c = getchar();
    }
    while (c >= '0' && c <= '9')
    {
        x = x * 10 + c - '0';
        c = getchar();
    }
    return x * f;
}
```

使用说明：
- `read()` 读 int，`readll()` 读 ll，均支持负数。
- 只处理十进制整数；需要读 char/string 时仍用 `scanf` 或 `cin`。

变体：
- 改为读无符号/大数只需去掉 f 分支。

---

## 128 位整数 IO（read128 / write128）

适用：数值超出 ll 范围（$10^{18}$ 级连加 / 相乘），或需要 $\text{ll}\times\text{ll}$ 的精确中间结果时。`__int128` 是 GCC 扩展，支持四则与比较，但 `scanf/printf` 都不能直接读写，必须自己写。需要时把本段拼在快读 read/readll 之后一起复制。

复杂度：$O(n)$。

```cpp
#define i128 __int128

inline i128 read128()
{
    i128 x = 0, f = 1;
    char c = getchar();
    while (c < '0' || c > '9')
    {
        if (c == '-') f = -1;
        c = getchar();
    }
    while (c >= '0' && c <= '9')
    {
        x = x * 10 + c - '0';
        c = getchar();
    }
    return x * f;
}

inline void write128(i128 x)
{
    if (x < 0)
    {
        putchar('-');
        x = -x;
    }
    if (x > 9) write128(x / 10);
    putchar(x % 10 + '0');
}
```

使用说明：
- `read128()` 读入、`write128(x)` 输出，均支持负数；`write128` 不自动换行，要换行时手动 `putchar('\n')`。
- 乘法防溢出必须先显式转一侧：`(i128)a * b`；直接 `a * b` 会在 ll 下先溢出再提升，结果已错。
- 比较、加减、取模原生支持；结果若确认在 ll 内可存回 ll，输出仍用 `write128`。

使用模板：

```cpp
ll a = readll(), b = readll();
i128 c = (i128)a * b;
write128(c);
putchar('\n');
```

变体：
- 只做中间量不输出时可只留 `read128` 一段。
- 无负数场景去掉 f 分支可省常数额外判断。

---

## 其他公共工具位置

- 快速幂 / 快速乘 `qpow/qmul`：见 `04-数论.md`。
- 扩展欧几里得 `exgcd`：见 `04-数论.md`。
- 通用头文件统一用 `#include <bits/stdc++.h>` + `using namespace std;`。

